-- Baseline migration — schema already applied directly to Supabase
-- All tables, enums, indexes and triggers were created via SQL API
-- This file exists so Prisma knows the migration baseline

-- Prisma baseline: schema applied 2026-03-27
SELECT 1;
